name: Gerar bipado direto no Rastreamento

on:
  push:
    branches:
      - manual/bipar-direto-rastreamento-20260730

permissions:
  contents: read

jobs:
  gerar:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Gerar arquivos corrigidos
        run: python gerar-bipar-direto-rastreamento.py

      - name: Validar sintaxe JavaScript
        run: |
          node --check app.js
          node --check sw.js
          node --check corponu-atualizador.js
          node --check corponu-rastreamento-interno.js

      - name: Validar escopo e proteções
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          versao = '2026-07-30-rastreamento-bipar-direto-12'
          app = Path('app.js').read_text(encoding='utf-8')
          regras = Path('firestore-rules.txt').read_text(encoding='utf-8')
          sw = Path('sw.js').read_text(encoding='utf-8')
          atualizador = Path('corponu-atualizador.js').read_text(encoding='utf-8')
          release = json.loads(Path('corponu-release.json').read_text(encoding='utf-8'))

          assert 'async function biparOrdemDireto(opId)' in app
          assert 'data-bipar-op-direto=' in app
          assert 'window.biparOrdemDireto = biparOrdemDireto;' in app
          assert 'const movimentacoesAbertas = state.movimentacoesProducao' in app
          assert 'const movimentacaoPrincipal = movimentacoesAbertas[0] || null;' in app
          assert 'movimentacoesAbertas.slice(1).forEach' in app
          assert 'encerradoPorBipadoDireto: true' in app
          assert 'Esta OP está vinculada a uma facção. Finalize pela aba Facções.' not in app
          assert 'const localEhFaccao' not in app
          assert 'localAtualMigracao: "FINALIZADO_BIPADO"' in app
          assert 'ocultarDoManejo: true' in app
          assert 'const lote = writeBatch(db);' in app
          assert 'lote.set(doc(db, "movimentacoesProducao"' in app
          assert 'lote.set(doc(db, "ordensProducao"' in app
          assert 'await lote.commit();' in app
          assert 'match /ordensProducao/{ordemId}' in regras
          assert 'match /movimentacoesProducao/{movimentoId}' in regras
          assert versao in sw
          assert versao in atualizador
          assert release['version'] == versao

          trecho = app[app.index('const ordensEmBipadoDireto'):app.index('function renderLinhaRastreamentoGlobalOP')]
          assert 'MutationObserver' not in trecho
          assert 'setInterval' not in trecho
          assert 'location.reload' not in trecho
          assert 'gerarPagamentoPorMovimentacao' not in trecho
          print('Validação rigorosa concluída.')
          PY

      - name: Conferir arquivos modificados pelo gerador
        run: |
          git diff --name-only | sort > alterados.txt
          printf '%s\n' app.js corponu-atualizador.js corponu-release.json sw.js | sort > esperados.txt
          diff -u esperados.txt alterados.txt

      - name: Montar pacote ZIP
        run: |
          mkdir pacote-bipar-direto-rastreamento
          cp app.js pacote-bipar-direto-rastreamento/
          cp sw.js pacote-bipar-direto-rastreamento/
          cp corponu-atualizador.js pacote-bipar-direto-rastreamento/
          cp corponu-release.json pacote-bipar-direto-rastreamento/
          cp LEIA-ME-BIPAR-DIRETO-RASTREAMENTO.txt pacote-bipar-direto-rastreamento/
          zip -r corponu-bipar-direto-rastreamento-12.zip pacote-bipar-direto-rastreamento

      - name: Publicar pacote para download
        uses: actions/upload-artifact@v4
        with:
          name: corponu-bipar-direto-rastreamento-12
          path: corponu-bipar-direto-rastreamento-12.zip
          if-no-files-found: error
          retention-days: 7
